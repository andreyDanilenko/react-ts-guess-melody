export enum AppRoute {
    LoginPage = '/login',
    GamePage = '/game',
    ResultPage = '/result',
    WelcomePage = '/',
  }

export enum AuthorizationStatus {
    Auth = 'AUTH',
    NoAuth = 'NO_AUTH',
    Unknown = 'UNKNOWN',
  }
